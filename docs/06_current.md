# 06 — Current task

<!-- Coherent description of the CURRENT active task. Not a dumping ground. If idle, write
     "between tasks — last completed X". Snapshot into 07_archive/06_YYYY-MM-DD_<tag>.md
     before a major pivot. -->

## 1. Task

On hold (2026-09-21) — resubmission of the three fine-tunes is explicitly paused pending new
data/details from the `openpi` runs, which the user is fetching. Last completed work: the
pre-fine-tuning review and first (partial) fine-tune attempt, archived at
[`07_archive/06_2026-09-17_review-and-first-finetune-attempt.md`](07_archive/06_2026-09-17_review-and-first-finetune-attempt.md).
`Ah_for_human.md`§3 Q6/Q7 are now answered and the resulting code is committed (see `0A_agent.md`§1).

When this resumes: re-check GPU availability, then resubmit the three fine-tunes per
`09_commands.md`§3 (`--time 48:00:00`, resume job 411 from its 130k checkpoint). Do not resubmit
before the user says the hold is lifted — see `Ah_for_human.md`§4.

## 2. Status

Paused, not blocked — waiting on the user to bring in `openpi`-derived data/details before
resubmission is cleared.
